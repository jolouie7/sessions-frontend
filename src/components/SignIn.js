import React, { Component } from 'react'

class SignIn extends Component {
  render() {
    return (
      <div>
        {/* If user is a valid user, sign them in and show the homePage/profilePage */}
      </div>
    )
  }
}

export default SignIn
